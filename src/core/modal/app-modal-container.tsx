'use client';

import { ModalContainer } from '../common/dynamic-modal';
import appModalControl from '.';

export default function AppModalContainer() {
  return <ModalContainer control={appModalControl} />;
}
